@isTest
private class Test_AIAWorkplanTypeStageTriggerHandler {

    static testMethod void testbeforeDelete() {
    /*comm as per us 210
       AIA_Workplan_Type__c wpt= AIA_TestUtil.createWPT();
       Workplan_Type_Stage__c wts= AIA_TestUtil.createWPTS(wpt.id);
       AIA_TestUtil.createWorkplan(wts);
        try{ 
       delete wts;
        }catch(Dmlexception e){
            System.assert(e.getMessage()!=null);
        }
*/
    }
    
}